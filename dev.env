# Development Environment Variables for Docker Container
# Phase 2: Replace .env mount with Docker-injected config
# This file is used by docker-compose.yml via env_file directive

# Bin Dataset Generation
ENABLE_BIN_DATASET=true

# Output Directory (local dev uses "reports", Cloud Run uses "/tmp/reports")
OUTPUT_DIR=reports

# Phase 3: GCS Upload Configuration
# To enable GCS uploads from local container:
# 1. Place service account JSON key in keys/gcs-sa.json
# 2. Uncomment and set GOOGLE_CLOUD_PROJECT to your project ID
# 3. Uncomment GOOGLE_APPLICATION_CREDENTIALS (path is set automatically)
# 4. Set GCS_UPLOAD=true
#
# GCS Upload (set to false for local-only, true if testing GCS from container)
GCS_UPLOAD=false

# Google Cloud Project (required for GCS uploads)
# Uncomment and set to your project ID when testing GCS
# GOOGLE_CLOUD_PROJECT=run-density

# Service Account Key Path (automatically set if keys/gcs-sa.json exists)
# The container mounts keys/ to /tmp/keys, so the key should be at /tmp/keys/gcs-sa.json
# GOOGLE_APPLICATION_CREDENTIALS=/tmp/keys/gcs-sa.json

# Bin Dataset Configuration (optional)
# BIN_MAX_FEATURES=1000
# DEFAULT_BIN_TIME_WINDOW_SECONDS=300

