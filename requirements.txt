# Web Runtime Dependencies for Run-Density (RF-FE-002)
# FastAPI multi-page web UI per Issue #279
# Baseline: v1.6.42 | Architecture: Option 3 - Hybrid Approach

# FastAPI Web Server
fastapi>=0.104.0
starlette>=0.27.0
uvicorn>=0.24.0
gunicorn>=21.2.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Data Models & Validation
pydantic>=2.5.0
typing-extensions>=4.8.0

# Configuration
pyyaml>=6.0

# Cloud Storage (GCS adapter for local = cloud parity)
google-cloud-storage>=2.10.0
google-auth>=2.23.0

# Analytics Runtime (density/flow calculations)
# Pinned to avoid dependency drift regressions (Issue #702 investigation)
pandas==2.3.3
numpy>=1.24.0

# HTTP Client (for health checks)
httpx>=0.24.0

# Short UUID generation (Epic #444)
shortuuid>=1.0.0

# Bin Dataset Support (Issue #198)
# Pinned to avoid dependency drift regressions (Issue #702 investigation)
pyarrow==22.0.0
shapely>=2.0.0
pyproj>=3.6.0
geopandas>=0.14.0

# Visualization libraries used only at export time (offline)
# Required for analytics/export_heatmaps.py (heatmap generation)
# Issue #280: Density Heatmaps & Captions from bin_summary.json
matplotlib>=3.8.0
pillow>=10.0.0
reportlab>=4.0.0

# Testing (Issue #535: E2E tests)
pytest>=7.4.0
requests>=2.31.0

# NOTE: Heavy dependencies removed from web runtime:
# - folium (static map generation → Leaflet client-side)
# - markdown (reports → Jinja templates)
# - pypandas (PDF generation → deferred)
