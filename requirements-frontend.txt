# Frontend-only dependencies (not needed for Cloud Run deployment)
# Used by: frontend/validation, frontend/map, frontend/dashboard, frontend/reports
# Installed in: CI workflows for map/validation/dashboard/reports generation only

# Phase 2: Static Map Generation (Issue #263)
folium>=0.17
matplotlib>=3.8

# Phase 4: Report Integration & Storytelling (Issue #265)
markdown>=3.6

# Phase 1: Data Contracts & Validation (Issue #262)
# Note: pydantic, jinja2, pyyaml, pandas, geopandas already in requirements-core.txt
