# Frontend-only dependencies (not needed for Cloud Run deployment)
# Used by: frontend/validation, frontend/map
# Installed in: CI workflows for map/validation generation only

# Phase 2: Static Map Generation (Issue #263)
folium>=0.17
matplotlib>=3.8

# Phase 1: Data Contracts & Validation (Issue #262)
# Note: pydantic, jinja2, pyyaml already in requirements-core.txt
